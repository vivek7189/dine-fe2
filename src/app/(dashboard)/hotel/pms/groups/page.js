'use client';
// Thin route wrapper — logic lives in src/features/hotel/.
import HotelGroups from '../../../../../features/hotel/HotelGroups';

export default function HotelPmsGroupsPage() {
  return <HotelGroups />;
}
