'use client';
// Thin route wrapper — logic lives in src/features/hotel/.
import HotelGuests from '../../../../../features/hotel/HotelGuests';

export default function HotelPmsGuestsPage() {
  return <HotelGuests />;
}
