'use client';
// Thin route wrapper — logic lives in src/features/hotel/.
import HotelCalendar from '../../../../../features/hotel/HotelCalendar';

export default function HotelPmsCalendarPage() {
  return <HotelCalendar />;
}
