<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Standard Android pattern for a POS that runs offline on a LAN:
      • Permit plain HTTP (cleartext) generally, so a terminal can reach the on-prem
        local server at an arbitrary private IP (http://192.168.x.x:3003 + ws://).
        HTTPS on a LAN IP is impractical (no CA issues certs for private IPs), which is
        why every POS — Toast, Square, Petpooja — uses HTTP on the local network.
      • FORCE HTTPS for our cloud endpoints so they can never be silently downgraded.
    -->
    <base-config cleartextTrafficPermitted="true" />

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">dineopen.com</domain>
        <domain includeSubdomains="true">vercel.app</domain>
        <domain includeSubdomains="true">run.app</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">firebaseio.com</domain>
        <domain includeSubdomains="true">firebasedatabase.app</domain>
    </domain-config>

    <!-- Emulator loopback stays cleartext for local dev. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
